{
  "name": "test-project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "deploy:local": "npm run build && sls deploy -c serverless.yml"
  },
  "devDependencies": {
    "serverless": "^4.18.2",
    "serverless-localstack": "^1.3.1",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
